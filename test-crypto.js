// Quick test script to verify crypto engine works
// Run with: node test-crypto.js

const crypto = require('crypto');

// Import the crypto functions (we'll need to adapt this for Node.js)
// For now, let's create a simplified test that uses the actual functions

async function testCrypto() {
  console.log('🧪 Testing Crypto Engine...\n');
  
  try {
    // We need to import the actual functions
    // Since this is a Next.js app, we'll need to use a different approach
    // Let's create a test that can run in Node.js
    
    console.log('⚠️  This test needs to run in the browser or use a different approach.');
    console.log('📝 Creating a browser-based test instead...\n');
    
  } catch (error) {
    console.error('❌ Test failed:', error);
    process.exit(1);
  }
}

// Actually, let's create an HTML test file instead that can be opened in browser
console.log('Creating browser test file...');

